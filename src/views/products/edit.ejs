<!DOCTYPE html>
<html lang="en">

   <%- include('../partials/head.ejs') %> 

<body>

    <%- include('../partials/headerAdmin.ejs', {producto}) %> 

   <main class="editMain">
      <h2>Editar producto</h2>
      <form class="editForm" action="edit/<%=producto.id %>?_method=put" method="POST">
         <div class="editInputContainer">
            <label for="nombre"> Nombre </label>
            <input type="text" name="nombre" id="nombre" value="<%= producto.nombre%>" required>
            <label for="descripcion">Descripción</label>
            <textarea name="descripcion" id="descripcion" value="<%= producto.descripcion%>" cols="30" rows="10"></textarea>
            <div class="editImages">
               <label for="imagen"> Imagen </label>
               <input type="file" name="imagen" id="imagen" accept=".png, .jpeg, .jpg" value="<%= producto.imagen%>" required>
               <label for="imagenDetalle"> Imagen detalle </label>
               <input type="file" name="imagenDetalle" id="imagenDetalle" accept=".png, .jpeg, .jpg" value="<%= producto.imagenDetalle%>" required>
            </div>
            <label for="tipo">Tipo de producto</label>
            <!-- <input type="text" name="tipo" id="tipo" value="< %= producto.tipo%>" required> -->
            <select name="tipo">
               <option value="planta" <%= producto.tipo == "planta" ? "selected" : "" %>> Planta </option>
               <option value="maceta" <%= producto.tipo == "maceta" ? "selected" : "" %>> Maceta </option>
               <option value="cuidados" <%= producto.tipo == "cuidados" ? "selected" : "" %>> Tips y cuidados </option>
            </select>
            <label>Tamaño</label>
            <select name="tamaño">
               <option value="small" <%= producto.tamaño == "small" ? "selected" : "" %>> S (Small) </option>
               <option value="medium" <%= producto.tamaño == "medium" ? "selected" : "" %>> M (Medium) </option>
               <option value="large" <%= producto.tamaño == "large" ? "selected" : "" %>> L (Large) </option>
               <option value="extra-large" <%= producto.tamaño == "extra-large" ? "selected" : "" %>> XL (Extra Large) </option>
            </select>
            <label for="precio">Precio</label>
            <input type="number" name="precio" id="precio" value="<%= producto.precio%>" required>
            <div class="editBenefits">
               <label for="cuotas">Cuotas</label>
               <input type="number" name="cuotas" id="cuotas" value="<%= producto.cuotas%>" required>
               <label>Categoría</label>
               <select name="categoria" >
                  <option value="visited" <%= producto.categoria == "visited" ? "selected" : "" %>> Últimos visitados </option>
                  <option value="inSale" <%= producto.categoria == "inSale" ? "selected" : "" %>> En oferta </option>
               </select>
            </div>
            </div>
         <button type="submit"> Guardar cambios </button>
      </form>
   </main>

   <%- include('../partials/footer.ejs') %> 
   
</body>
</html>